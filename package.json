{
  "name": "waypoint-addon",
  "version": "1.0.0",
  "description": "A Minecraft Bedrock addon for waypoints",
  "scripts": {
    "build": "tsc && node scripts/copy-assets.js",
    "watch": "tsc -w"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "pngjs": "^7.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@minecraft/server": "^1.12.0",
    "@minecraft/server-ui": "^1.2.0"
  }
}
